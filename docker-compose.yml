services:
  physics-sim:
    build: .
    image: physics-sim:latest
    container_name: physics-sim-container
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
